setlocal colorcolumn=80 " highlight column 80

setlocal tabstop=4 " number of spaces per TAB when reading a file
setlocal shiftwidth=4 " number of spaces when indenting with >
setlocal expandtab " when using tab, insert spaces
